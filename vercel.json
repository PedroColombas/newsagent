{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    {
      "comment": "The site root is the landing page — that is the link that goes on a CV.",
      "source": "/",
      "destination": "/landing/index.html"
    },
    {
      "comment": "The app lives under /app. Every route below it falls through to the SPA shell, which React Router then reads (see lib/routes.ts).",
      "source": "/app",
      "destination": "/index.html"
    },
    {
      "source": "/app/(.*)",
      "destination": "/index.html"
    }
  ]
}
