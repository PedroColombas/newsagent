# Frontend (Vite) env. VITE_-prefixed vars are bundled into the browser — only PUBLIC
# values belong here. The Supabase anon key is public (client-side); the service_role
# key must NEVER appear in the frontend.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
