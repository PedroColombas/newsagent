import { PagePlaceholder } from "../components/PagePlaceholder";

export function Preferences() {
  return (
    <PagePlaceholder
      title="Preferences"
      note="Topics, report mode, voice, recency, delivery — the core screen, coming in Phase 5."
    />
  );
}
