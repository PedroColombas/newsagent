# Trigger.dev pipeline secrets. Set the SAME values in BOTH the Dev and Prod
# environments in the Trigger.dev dashboard (Trigger §5 — env vars are per-environment).
# These are server-side only — never expose them to the frontend.

# Supabase (service_role bypasses RLS — keep secret)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# LLM / news providers
ANTHROPIC_API_KEY=
PERPLEXITY_API_KEY=
OPENAI_API_KEY=
